# generated by tools/firmware-sync.js from layouts/go60.keymap — CI syncs it
# itself, so committing it would just be a second copy that can drift
config/go60.keymap
# build outputs
go60.uf2
combined
# moergo-sc/zmk checkout (Actions puts it here; so can you, for bare nix builds)
src/
